import 'package:flutter/material.dart';

class KostDetailPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Kost'),
      ),
      body: Center(
        child: Text('Kost Detail Content'),
      ),
    );
  }
}
